#include "main"

/**
 * _strlen - return the length of a string
 *
 *@s: string 
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int len, i;

	for (len = 0; s[len] != '\0'; len++)
	{
		i = len + 1;
	}
	return i;
}
